{% sw_extends '@Storefront/storefront/page/checkout/confirm/confirm-item.html.twig' %}

{% block page_checkout_item_tax_price_inner %}
    {% if lineItem.extension('customBonusSystem') %}
        {{ lineItem.extension('customBonusSystem').single }}P
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
